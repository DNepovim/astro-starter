---
type Props = {
  src: string
  alt: string
}

const { src, alt } = Astro.props
---

<img
  src={src}
  alt={alt}
  class="h-full w-full rounded-lg"
  width={544}
  height={350}
  loading="lazy"
/>
